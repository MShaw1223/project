import React from "react"

interface DUinterface {
    user: string
}

//interface to take the string and pass it from the delete user form
//and to the postgres

export default function DeleteUser(){
    return(
        <h1>
            This is where I delete a user 
        </h1>
    )
}